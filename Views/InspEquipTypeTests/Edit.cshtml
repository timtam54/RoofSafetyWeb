@model RoofSafety.Models.InspEquipTypeTestRpt

@{
    ViewData["Title"] = "Test";
}

<h1>Test</h1>
<hr />
<div class="row" style="width:100%">
    <div class="col-md-4" style="width:100%">
        <form asp-action="Edit" style="width:100%">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="id" />
            <div class="form-group" hidden="hidden">
                <label asp-for="InspEquipID" class="control-label"></label>
                <input asp-for="InspEquipID" class="form-control" />
                <span asp-validation-for="InspEquipID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EquipTypeTestID" class="control-label"></label>
                <select asp-for="EquipTypeTestID" class="form-control" style="width:100%" asp-items="@(new SelectList(ViewBag.EquipTypeTestID, "Value", "Text"))"></select>
                <a asp-action="HazardsForEquipTypeTestHazards" asp-controller="EquipTypeTestHazards" asp-route-id="@Model.EquipTypeTestID" asp-route-ieetid="@Model.EquipTypeTestID">Hazards</a>

            </div>
       @*      <div class="form-group">
                <label asp-for="Pass" class="control-label"></label>
         
            </div> *@
            <div>
                <ul>

                    @foreach (var item in Model.Reason)
                    {
                        <li>@item</li>

                    }
                </ul>
            </div>

            <!--div class="form-group">
        <label asp-for="Reason" class="control-label"></label>
        <input asp-for="Reason" class="form-control" />
        <span asp-validation-for="Reason" class="text-danger"></span>
    </div-->
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="TestsForEquip" asp-route-id="@Model.InspEquipID">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
